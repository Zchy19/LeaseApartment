<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zchy.lease.web.app.mapper.PaymentTypeMapper">

    <select id="getListByRoomId" resultType="com.zchy.lease.model.entity.PaymentType">
        select *
        from lease.payment_type pt
        where pt.id in (
            select rpt.payment_type_id
            from lease.room_payment_type rpt
            where rpt.room_id = #{id}
            and rpt.is_deleted = 0
            )
        and pt.is_deleted = 0
    </select>
    <select id="selectListByRoomId" resultType="com.zchy.lease.model.entity.PaymentType">
        select id,
               type,
               name
        from label_info
        where is_deleted = 0
          and id in (select label_id
                     from room_label
                     where is_deleted = 0
                       and room_id = #{id})
    </select>
</mapper>